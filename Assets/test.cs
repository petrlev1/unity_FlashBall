using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
		
		int i3 = 0;
		for ( i3=0; i3<100; i3++ ) { Debug.Log(i3); }
        
    }

    // Update is called once per frame
    void Update()
    {
		
		
        
    }
}
